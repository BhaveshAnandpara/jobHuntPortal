# Copy to .env.local for local development (gitignored by Vite's default
# .gitignore). See docs/frontend/api-mapping.md's header note: the backend
# is mounted with no route prefix today (not /api/v1/...), so this should
# point at the bare FastAPI server root.
VITE_API_BASE_URL=http://localhost:8000
