__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

.venv/
venv/
env/

.env
# Production secrets for the beta deployment (docs/deployment/TECH.md Task 6),
# referenced by docker-compose.data.yml and docker-compose.app.yml — each VM
# gets its own. The `.env` line above matches only a file named exactly `.env`,
# so this needs its own entry.
.env.production
*.db
*.sqlite3

.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

.vscode/
.idea/

# Runtime application logs (infrastructure/logging) — never committed.
logs/
*.log

# Uploaded resume files (profiles/storage.py's LocalResumeStorage) — user
# data, never committed.
data/resumes/
