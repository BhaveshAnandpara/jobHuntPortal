"""Shared contracts used by every component.

Nothing in this package is owned by any single component — see
docs/architecture/repository-structure.md. No component may define its own
version of a type that already exists under `shared/`.
"""
