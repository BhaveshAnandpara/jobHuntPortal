"""Shared pytest fixtures. No fixtures defined yet — add fixtures here only
once they are shared by more than one component's tests; component-specific
fixtures belong in that component's own test package.
"""
